Logistics
￼
Great — we’ll do this **strictly file by file, in chronological order** from here on.  
I’ll start with **Part 1**: the project foundation files that everything else depends on. Flutter’s `MaterialApp.router` setup and localization delegates are the correct base for this app, and `go_router` is the right routing layer for the navigation structure you’ve been building.[1][2][3][4]

## Part 1 — Foundation

### `pubspec.yaml`
```yaml
name: global_cloud
description: Global Cloud Logistics App
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  go_router: ^14.8.1
  dio: ^5.7.0
  shared_preferences: ^2.3.2
  flutter_secure_storage: ^9.2.2
  intl: ^0.19.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    - assets/lottie/
    - assets/translations/
```

### `lib/main.dart`
```dart
import 'package:flutter/material.dart';
import 'app.dart';

void main() {
  WidgetsFlutterBinding.ensureInitialized();
  runApp(const GlobalCloudApp());
}
```

### `lib/app.dart`
```dart
import 'package:flutter/material.dart';
import 'package:flutter_localizations/flutter_localizations.dart';
import 'core/localization/app_localizations_delegate.dart';
import 'core/localization/supported_locales.dart';
import 'core/routing/app_router.dart';
import 'core/theme/app_theme.dart';

class GlobalCloudApp extends StatelessWidget {
  const GlobalCloudApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp.router(
      debugShowCheckedModeBanner: false,
      title: 'Global Cloud',
      theme: AppTheme.darkTheme,
      routerConfig: appRouter,
      supportedLocales: SupportedLocales.locales,
      localizationsDelegates: const [
        AppLocalizationsDelegate(),
        GlobalMaterialLocalizations.delegate,
        GlobalWidgetsLocalizations.delegate,
        GlobalCupertinoLocalizations.delegate,
      ],
    );
  }
}
```

### `lib/core/constants/app_colors.dart`
```dart
import 'package:flutter/material.dart';

class AppColors {
  static const bg = Color(0xFF071A2B);
  static const surface = Color(0xFF0E2740);
  static const card = Color(0xFF12314E);
  static const primary = Color(0xFF2FA8D8);
  static const secondary = Color(0xFF67D4FF);
  static const text = Color(0xFFF4F8FB);
  static const muted = Color(0xFFA8C0D6);
  static const success = Color(0xFF2ECC71);
  static const warning = Color(0xFFF5B041);
  static const error = Color(0xFFE74C3C);
}
```

### `lib/core/theme/app_theme.dart`
```dart
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';
import '../constants/app_colors.dart';

class AppTheme {
  static ThemeData get darkTheme {
    final base = ThemeData.dark(useMaterial3: true);

    return base.copyWith(
      scaffoldBackgroundColor: AppColors.bg,
      colorScheme: base.colorScheme.copyWith(
        primary: AppColors.primary,
        secondary: AppColors.secondary,
        surface: AppColors.surface,
      ),
      textTheme: GoogleFonts.poppinsTextTheme(base.textTheme).apply(
        bodyColor: AppColors.text,
        displayColor: AppColors.text,
      ),
      appBarTheme: const AppBarTheme(
        backgroundColor: AppColors.bg,
        foregroundColor: AppColors.text,
        centerTitle: true,
      ),
      cardTheme: CardTheme(
        color: AppColors.card,
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.circular(20),
        ),
      ),
      inputDecorationTheme: InputDecorationTheme(
        filled: true,
        fillColor: AppColors.surface,
        border: OutlineInputBorder(
          borderRadius: BorderRadius.circular(16),
          borderSide: BorderSide.none,
        ),
      ),
    );
  }
}
```

### `lib/core/localization/supported_locales.dart`
```dart
import 'package:flutter/material.dart';

class SupportedLocales {
  static const locales = <Locale>[
    Locale('en'),
    Locale('ar'),
    Locale('ku'),
  ];
}
```

### `lib/core/localization/app_localizations_delegate.dart`
```dart
import 'package:flutter/material.dart';
import 'app_localizations.dart';

class AppLocalizationsDelegate extends LocalizationsDelegate<AppLocalizations> {
  const AppLocalizationsDelegate();

  @override
  bool isSupported(Locale locale) {
    return ['en', 'ar', 'ku'].contains(locale.languageCode);
  }

  @override
  Future<AppLocalizations> load(Locale locale) async {
    return AppLocalizations(locale);
  }

  @override
  bool shouldReload(covariant LocalizationsDelegate<AppLocalizations> old) => false;
}
```

### `lib/core/localization/app_localizations.dart`
```dart
import 'package:flutter/material.dart';

class AppLocalizations {
  final Locale locale;

  const AppLocalizations(this.locale);

  static const Map<String, Map<String, String>> _values = {
    'en': {
      'appName': 'Global Cloud',
      'welcome': 'Welcome Back',
      'login': 'Login',
      'register': 'Register',
      'home': 'Home',
      'tracking': 'Tracking',
      'orders': 'Orders',
      'invoices': 'Invoices',
      'notifications': 'Notifications',
      'profile': 'Profile',
      'support': 'Support',
      'logout': 'Logout',
      'language': 'Language',
      'trackShipment': 'Track Shipment',
      'submitTicket': 'Submit Ticket',
    },
    'ar': {
      'appName': 'الغيوم العالمية',
      'welcome': 'مرحباً بعودتك',
      'login': 'تسجيل الدخول',
      'register': 'إنشاء حساب',
      'home': 'الرئيسية',
      'tracking': 'التتبع',
      'orders': 'الطلبات',
      'invoices': 'الفواتير',
      'notifications': 'الإشعارات',
      'profile': 'الملف الشخصي',
      'support': 'الدعم',
      'logout': 'تسجيل الخروج',
      'language': 'اللغة',
      'trackShipment': 'تتبع الشحنة',
      'submitTicket': 'إرسال التذكرة',
    },
    'ku': {
      'appName': 'هەورەکانی جیهانی',
      'welcome': 'بەخێربێیتەوە',
      'login': 'چوونەژوورەوە',
      'register': 'تۆمارکردن',
      'home': 'سەرەکی',
      'tracking': 'شوێنپێکەوتن',
      'orders': 'داواکارییەکان',
      'invoices': 'فاکتۆرەکان',
      'notifications': 'ئاگادارییەکان',
      'profile': 'پرۆفایل',
      'support': 'پشتیوانی',
      'logout': 'چوونەدەرەوە',
      'language': 'زمان',
      'trackShipment': 'دوای شەحنە بکە',
      'submitTicket': 'ناردنی تیکەت',
    },
  };

  String t(String key) {
    return _values[locale.languageCode]?[key] ?? _values['en']![key] ?? key;
  }

  static AppLocalizations of(BuildContext context) {
    final locale = Localizations.localeOf(context);
    return AppLocalizations(locale);
  }
}
```

### `lib/core/routing/route_paths.dart`
```dart
class RoutePaths {
  static const splash = '/';
  static const onboarding = '/onboarding';
  static const login = '/login';
  static const register = '/register';
  static const home = '/home';
  static const tracking = '/tracking';
  static const trackingDetails = '/tracking-details';
  static const orders = '/orders';
  static const invoices = '/invoices';
  static const invoiceDetails = '/invoice-details';
  static const notifications = '/notifications';
  static const profile = '/profile';
  static const support = '/support';
}
```

### `lib/core/routing/app_router.dart`
```dart
import 'package:go_router/go_router.dart';
import '../../features/auth/login_screen.dart';
import '../../features/auth/register_screen.dart';
import '../../features/home/home_screen.dart';
import '../../features/invoices/invoices_screen.dart';
import '../../features/notifications/notifications_screen.dart';
import '../../features/onboarding/onboarding_screen.dart';
import '../../features/orders/orders_screen.dart';
import '../../features/profile/profile_screen.dart';
import '../../features/shell/main_shell.dart';
import '../../features/splash/splash_screen.dart';
import '../../features/support/support_screen.dart';
import '../../features/tracking/tracking_details_screen.dart';
import '../../features/tracking/tracking_screen.dart';
import 'route_paths.dart';

final GoRouter appRouter = GoRouter(
  initialLocation: RoutePaths.splash,
  routes: [
    GoRoute(
      path: RoutePaths.splash,
      builder: (context, state) => const SplashScreen(),
    ),
    GoRoute(
      path: RoutePaths.onboarding,
      builder: (context, state) => const OnboardingScreen(),
    ),
    GoRoute(
      path: RoutePaths.login,
      builder: (context, state) => const LoginScreen(),
    ),
    GoRoute(
      path: RoutePaths.register,
      builder: (context, state) => const RegisterScreen(),
    ),
    ShellRoute(
      builder: (context, state, child) {
        return MainShell(child: child);
      },
      routes: [
        GoRoute(
          path: RoutePaths.home,
          builder: (context, state) => const HomeScreen(),
        ),
        GoRoute(
          path: RoutePaths.orders,
          builder: (context, state) => const OrdersScreen(),
        ),
        GoRoute(
          path: RoutePaths.tracking,
          builder: (context, state) => const TrackingScreen(),
        ),
        GoRoute(
          path: RoutePaths.invoices,
          builder: (context, state) => const InvoicesScreen(),
        ),
        GoRoute(
          path: RoutePaths.notifications,
          builder: (context, state) => const NotificationsScreen(),
        ),
        GoRoute(
          path: RoutePaths.profile,
          builder: (context, state) => const ProfileScreen(),
        ),
        GoRoute(
          path: RoutePaths.support,
          builder: (context, state) => const SupportScreen(),
        ),
      ],
    ),
    GoRoute(
      path: RoutePaths.trackingDetails,
      builder: (context, state) => TrackingDetailsScreen(
        shipment: state.extra as dynamic,
      ),
    ),
  ],
);
```

If you want, I’ll continue immediately with **Part 2** and generate the next files in order: the remaining core utilities and shared widgets.

Sources
[1] localizationsDelegates property - MaterialApp class - material library https://api.flutter.dev/flutter/material/MaterialApp/localizationsDelegates.html
[2] MaterialApp.router constructor - Dart API https://api.flutter.dev/flutter/material/MaterialApp/MaterialApp.router.html
[3] go_router install | Flutter package https://pub.dev/packages/go_router/install
[4] Internationalizing Flutter apps https://docs.flutter.dev/ui/internationalization
[5] pubspec.yaml https://gist.github.com/vbalagovic/551560c459d2cb141c3c0c2ef9d184e9
[6] Navigate with named routes - Flutter documentation https://docs.flutter.dev/cookbook/navigation/named-routes
[7] pod_router install | Flutter package https://pub.dev/packages/pod_router/install
[8] How to add new localization language that don't supported ... - GitHub https://github.com/flutter/flutter/issues/30187
[9] Navigation and routing - Flutter documentation https://docs.flutter.dev/ui/navigation
[10] flutter-project-template-riverpod/pubspec.yaml at master · brizaldi/flutter-project-template-riverpod https://github.com/brizaldi/flutter-project-template-riverpod/blob/master/pubspec.yaml
